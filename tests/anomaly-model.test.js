"use strict"; const test=require("node:test"),assert=require("node:assert/strict"),{data,admission,read}=require("../tools/intake-lib");
test("anomaly records remain source-local and interaction-linked",()=>{const observations=read("anomalies/observations.json").observations,interactions=read("anomalies/interactions.json").interactions;const ids=new Set(interactions.map(({id})=>id));for(const observation of observations) for(const id of observation.interaction_refs) assert.ok(ids.has(id));assert.equal(observations[0].boundary_state,"marked");assert.notEqual(observations[0].boundary_state,"exact");});
test("destination, mechanism, causal sequence, and community shorthand cannot become truth",()=>{const {claimById}=data();for(const id of ["ff2-testing-caused-transition-is-rejected","ff2-is-not-a-universal-anomaly-rule","noclip-is-not-authoritative-ff2-mechanism"]) assert.deepEqual(admission({use:"authoritative-world-state"},claimById.get(id)),{ok:false,code:"PROHIBITED_CLAIM"});assert.equal(claimById.get("ff2-destination-and-mechanism-remain-unresolved").review_state,"needs-context");});
