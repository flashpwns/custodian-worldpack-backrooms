"use strict";const test=require("node:test"),assert=require("node:assert/strict"),{startRun}=require("../tools/run-bootstrap");for(const profile of ["async-command","field-researcher","local-anomaly","lost"])test(`${profile} bootstraps deterministically`,()=>{const a=startRun({profile,seed:"same"}),b=startRun({profile,seed:"same"});assert.ok(a.ok&&b.ok);assert.equal(a.session.id,b.session.id);assert.equal(a.restored_equivalent,true);});test("institutional knowledge remains profile-local",()=>{const command=startRun({profile:"async-command"}),lost=startRun({profile:"lost"});assert.ok(command.summary.knowledge.length>lost.summary.knowledge.length);assert.equal(lost.summary.knowledge.length,0);});
