"use strict"; const test=require("node:test"),assert=require("node:assert/strict"),{data,admission,read}=require("../tools/intake-lib");
test("survey records preserve qualitative and numeric boundaries",()=>{const surveys=read("environment/surveys.json").observations;assert.ok(surveys.every((s)=>!s.unit||s.reported_value!==undefined));assert.ok(surveys.every((s)=>s.reported_value===undefined));});
test("negative environmental inferences cannot become authoritative",()=>{const {claimById}=data();assert.deepEqual(admission({use:"authoritative-world-state"},claimById.get("complex-lighting-is-not-a-global-grid-law")),{ok:false,code:"PROHIBITED_CLAIM"});assert.equal(claimById.get("lighting-survey-does-not-provide-a-numeric-material-measurement").review_state,"needs-context");});
test("survey links remain local and preserve material uncertainty",()=>{const conditions=read("environment/conditions.json").conditions;const fixture=conditions.find(({id})=>id==="lighting-survey-local-fixture");assert.equal(fixture.material,"unknown-material");assert.equal(fixture.target_environment,"lighting-survey-local-interior");assert.ok(!conditions.some(({state})=>state==="globally-synchronized"));});
