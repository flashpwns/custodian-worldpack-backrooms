# Developer Bootstrap Tutorial

Run `npm run start-run -- --profile lost --seed example-seed`. Choose
`async-command`, `field-researcher`, `local-anomaly`, or `lost`. This uses
Custodian's package-root `createSession` API and prints only player-local
startup information. `npm run run-report` verifies export/restore. This is a
developer bootstrap, not an interactive player UI.
