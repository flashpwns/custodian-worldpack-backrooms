# YB-32 Pass 2: Discovery density

Pass 2 makes existing observer-safe material easier to notice and carry forward. The v2 environment generator already materializes bounded environmental distinctions, local landmarks, and mundane objects; the status projection now preserves those fields for scene presentation, and Lost exposes its existing local landmark, sensory, and object projection.

No new generator, loot table, anomaly admission, quest, entity, taxonomy, or canonical authority was added. The report samples five deterministic seeds in Clear-Q4, Lost, Nullzone, and Beck. Clear-Q4 and Lost traverse only already-generated exits; Nullzone reports its existing access question and remembered routes; Beck reports its existing role/resource context.

The pre-change scene surfaces contained zero local landmark/object notices even when v2 had materialized them. After the projection change, `npm run discovery-density-report` measures the resulting observer-safe notices and averages. Discovery remains derived from canonical state and bounded presentation; source-backed phenomena still require explicit admission and history.
