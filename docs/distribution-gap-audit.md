# Distribution gap audit

Critical: Node runtime and a reproducible Custodian dependency remain required.
Important: package a `play` CLI, choose a user save directory, and test npm
installation. Nice-to-have: standalone macOS/Windows bundles and a desktop UI.
Future AI provider configuration must keep keys outside saves; offline
structured commands remain the fallback.
