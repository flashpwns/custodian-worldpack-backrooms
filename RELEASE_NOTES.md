# Yellow Beast 0.2.0-alpha.1

Async: Clear-Q4 is playable with LOOK, MOVE, INSPECT, USE, save/resume, structured commands, and the offline interpreter. OpenAI is optional when `OPENAI_API_KEY` is configured. macOS and Windows artifacts are unsigned alpha builds. Other modes are experimental; no final GUI, installer, or stable save-format guarantee is claimed.
