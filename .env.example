# Optional future provider credentials belong in the environment, never in saves.
# YELLOW_BEAST_AI_PROVIDER=example-provider
# YELLOW_BEAST_AI_MODEL=example-model
# YELLOW_BEAST_AI_API_KEY=replace-with-your-key
