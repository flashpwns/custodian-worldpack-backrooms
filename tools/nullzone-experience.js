"use strict";
const VERSION = "yellow-beast-nullzone-experience@v1";
const phases = require("./mode-phases");
function presentation(world, phase, local) { const base = local.base ?? {}; const investigation = local.investigation ?? {}; const archive = (base.archived_artifacts ?? []).map((item, index) => ({ entry_id: `object-${index + 1}`, category: "recovered object", label: item.type === "unidentified-object" ? "recovered object" : "archived item", provenance: "personal recovery" })); return { version: VERSION, phase: phase.phase_id, home_context: phase.phase_id === "HOME" ? "Your base holds the material you chose to keep." : null, archive, notebook: { questions: investigation.unresolved ?? [], hypotheses: (investigation.hypotheses ?? []).map((item) => ({ label: "personal hypothesis", state: item.state })) }, routes: (local.current_excursion?.known_routes ?? []).map((route, index) => ({ label: index ? `remembered route ${index + 1}` : "access hall", certainty: "remembered" })), guidance: phase.tutorial_context.enabled && !phase.tutorial_context.completed ? "Look over what you have recorded, then decide what you want to examine or take with you." : null }; }
function nextPhase(phase, action) { const map = { HOME: "EVIDENCE_REVIEW", EVIDENCE_REVIEW: "PREPARATION", PREPARATION: "EXCURSION", EXCURSION: "RETURN", RETURN: "COMPARISON", COMPARISON: "HOME" }; return phases.transition(phase, map[phase.phase_id], { reason: action ?? "personal-investigation", guard: Boolean(map[phase.phase_id]) }); }
module.exports = { VERSION, presentation, nextPhase };
