"use strict";
const crypto = require("node:crypto"); const history = require("./world-history"); const clone = (value) => structuredClone(value); const hash = (value) => crypto.createHash("sha256").update(JSON.stringify(value)).digest("hex");
const VERSION = "yellow-beast-entity-simulation@v1";
const entityId = (world, type, region_id, space_id, origin) => `entity-${hash([world.world_id, type, region_id, space_id, origin]).slice(0, 16)}`;
const hazardId = (world, type, region_id, space_id, origin) => `hazard-${hash([world.world_id, type, region_id, space_id, origin]).slice(0, 16)}`;
const encounterId = (world, run_id, observer, subject) => `encounter-${hash([world.world_id, run_id, observer, subject]).slice(0, 16)}`;
function state(world) { history.assertWorld(world); world.entities ??= {}; world.hazards ??= {}; world.encounters ??= {}; return world; }
function addEntity(world, { run_id, type, region_id, space_id, definition_id, authority, provenance, behavior_state = "stationary", perception_channels = [] }) { state(world); if (type === "still-life" && (perception_channels.length || behavior_state !== "stationary")) return { ok: false, code: "STILL_LIFE_BEHAVIOR_PROHIBITED" }; const region = world.regions[region_id]; if (!region?.state.nodes[space_id]) return { ok: false, code: "ENTITY_LOCATION_UNKNOWN" }; const id = entityId(world, type, region_id, space_id, definition_id); if (world.entities[id]) return { ok: true, idempotent: true, entity: clone(world.entities[id]) }; const entity = { id, type, world_id: world.world_id, region_id, space_id, active: true, physical_state: "present", behavior_state, definition_id, authority, provenance, origin_run: run_id, perception: { channels: [...new Set(perception_channels)], status: "unaware" } }; world.entities[id] = entity; history.event(world, run_id, "entity.present", { entity_id: id, type, region_id, space_id, definition_id, authority, provenance }, authority); return { ok: true, entity: clone(entity) }; }
function addHazard(world, { run_id, type, region_id, space_id, authority, provenance, effect = "avoidance-required", warning = "local-warning" }) { state(world); const region = world.regions[region_id]; if (!region?.state.nodes[space_id]) return { ok: false, code: "HAZARD_LOCATION_UNKNOWN" }; const id = hazardId(world, type, region_id, space_id, provenance); if (world.hazards[id]) return { ok: true, idempotent: true, hazard: clone(world.hazards[id]) }; const hazard = { id, type, world_id: world.world_id, region_id, space_id, active: true, authority, provenance, effect, warning, origin_run: run_id, discoverability: "local-perception" }; world.hazards[id] = hazard; history.event(world, run_id, "hazard.present", { hazard_id: id, type, region_id, space_id, authority, provenance }, authority); return { ok: true, hazard: clone(hazard) }; }
function localItems(world, region_id, space_id) { state(world); return { entities: Object.values(world.entities).filter((item) => item.active && item.region_id === region_id && item.space_id === space_id), hazards: Object.values(world.hazards).filter((item) => item.active && item.region_id === region_id && item.space_id === space_id) }; }
function resolveObserverPerception(world, { run_id, observer, region_id, space_id }) { const { entities, hazards } = localItems(world, region_id, space_id); const visible_entities = entities.map((item) => ({ kind: item.type, presence: "visible-physical-presence" })); const local_hazards = hazards.map((item) => ({ type: item.type, warning: item.warning, effect: item.effect })); const perceived = { visible_entities, perceived_traces: [], local_hazards, available_responses: [...(visible_entities.length ? ["RETREAT", "WAIT", "OBSERVE"] : []), ...(local_hazards.length ? ["AVOID", "CONTINUE", "RETREAT", "WAIT"] : [])] };
  for (const item of entities) history.event(world, run_id, "entity.perceived", { entity_id: item.id, region_id, space_id, observer }, item.authority);
  for (const item of hazards) history.event(world, run_id, "hazard.perceived", { hazard_id: item.id, region_id, space_id, observer }, item.authority);
  return perceived;
}
function entityPerception(world, { entity_id, observer, region_id, space_id }) { const entity = world.entities?.[entity_id]; if (!entity) return { detected: false, code: "ENTITY_UNKNOWN" }; const permitted = entity.perception.channels.includes("visual") && entity.region_id === region_id && entity.space_id === space_id; if (permitted) entity.perception.status = "detected-observer"; return { detected: permitted }; }
function beginEncounter(world, { run_id, observer, region_id, space_id, subject_type, subject_id }) { state(world); const subject = subject_type === "entity" ? world.entities[subject_id] : world.hazards[subject_id]; if (!subject || !subject.active || subject.region_id !== region_id || subject.space_id !== space_id) return { ok: false, code: "ENCOUNTER_TRIGGER_INVALID" }; const id = encounterId(world, run_id, observer, subject_id); if (world.encounters[id]) return { ok: true, idempotent: true, encounter: clone(world.encounters[id]) }; const entity_detection = subject_type === "entity" ? entityPerception(world, { entity_id: subject_id, observer, region_id, space_id }).detected : false; const encounter = { id, world_id: world.world_id, run_id, observer, region_id, space_id, subject_type, subject_id, observer_detected: true, entity_detected_observer: entity_detection, status: "active", options: subject_type === "entity" ? ["RETREAT", "WAIT", "OBSERVE"] : ["AVOID", "CONTINUE", "RETREAT", "WAIT"], outcome: null }; world.encounters[id] = encounter; history.event(world, run_id, "encounter.started", { encounter_id: id, subject_type, subject_id, region_id, space_id, observer_detected: true, entity_detected_observer: entity_detection }, subject.authority); return { ok: true, encounter: clone(encounter) }; }
function connected(world, region_id, from, to) { const region = world.regions[region_id]; return Object.values(region?.state.edges ?? {}).some((edge) => edge.traversable && ((edge.from === from && edge.to === to) || (edge.to === from && edge.from === to))); }
function resolveEncounter(world, { run_id, encounter_id, action, retreat_space_id = null, known_spaces = [] }) { const encounter = world.encounters?.[encounter_id]; if (!encounter || encounter.status !== "active" || encounter.run_id !== run_id) return { ok: false, code: "ENCOUNTER_UNAVAILABLE" }; if (!encounter.options.includes(action)) return { ok: false, code: "ENCOUNTER_OPTION_UNAVAILABLE" }; if ((action === "RETREAT" || action === "AVOID") && (!retreat_space_id || !known_spaces.includes(retreat_space_id) || !connected(world, encounter.region_id, encounter.space_id, retreat_space_id))) return { ok: false, code: "RETREAT_ROUTE_UNKNOWN" }; encounter.status = "resolved"; encounter.outcome = action === "WAIT" ? "observed-no-authorized-change" : action === "OBSERVE" ? "observed" : action === "CONTINUE" ? "exposed" : "avoided"; if (action === "CONTINUE" && encounter.subject_type === "hazard") history.event(world, run_id, "hazard.exposed", { encounter_id, hazard_id: encounter.subject_id, outcome: encounter.outcome }, "scenario-optional"); else history.event(world, run_id, action === "WAIT" ? "encounter.waited" : action === "OBSERVE" ? "encounter.observed" : action === "RETREAT" ? "encounter.retreat" : "hazard.avoided", { encounter_id, subject_type: encounter.subject_type, subject_id: encounter.subject_id, outcome: encounter.outcome }, "scenario-optional"); return { ok: true, encounter: clone(encounter), location: retreat_space_id ?? encounter.space_id }; }
function localProjection(world, { region_id, space_id, encounter_id = null }) { const { entities, hazards } = localItems(world, region_id, space_id); const visible_entities = entities.map((item) => ({ kind: item.type, presence: "visible-physical-presence" })); const local_hazards = hazards.map((item) => ({ type: item.type, warning: item.warning, effect: item.effect })); const encounter = encounter_id && world.encounters?.[encounter_id]; const available_responses = [...(visible_entities.length ? ["RETREAT", "WAIT", "OBSERVE"] : []), ...(local_hazards.length ? ["AVOID", "CONTINUE", "RETREAT", "WAIT"] : [])]; return { visible_entities, perceived_traces: [], local_hazards, encounter_state: encounter ? { status: encounter.status, observer_detected: encounter.observer_detected, available_responses: encounter.status === "active" ? encounter.options : [] } : null, available_responses }; }
function institutionalProjection(world) { state(world); return Object.values(world.knowledge.institutional.records).filter((record) => record.id.startsWith("institutional-entity-") || record.id.startsWith("institutional-hazard-")).map((record) => clone(record.payload)); }
function reportEntity(world, { run_id, entity_id, summary }) { const entity = world.entities?.[entity_id]; if (!entity) return { ok: false, code: "ENTITY_UNKNOWN" }; const id = `institutional-entity-${entity_id}`; if (!world.knowledge.institutional.records[id]) { world.knowledge.institutional.records[id] = { id, source_run: run_id, status: "archived-operational", payload: { type: entity.type, summary } }; history.event(world, run_id, "entity.reported", { entity_id, summary }, "scenario-optional"); } return { ok: true }; }
module.exports = { VERSION, state, addEntity, addHazard, resolveObserverPerception, entityPerception, beginEncounter, resolveEncounter, localProjection, institutionalProjection, reportEntity };
