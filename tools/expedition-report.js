"use strict";
const { startRun, act, look, saveRun, resumeRun } = require("./run-bootstrap");
function smooth(seed) { let run = startRun({ profile: "field-researcher", seed }).run; act(run, "MOVE"); const alias = look(run).aliases[0].alias; act(run, "INSPECT", alias); run = resumeRun(saveRun(run)).run; act(run, "RECORD", alias); act(run, "COMMUNICATE", "standard"); act(run, "USE", "survey-instrument"); act(run, "RETURN"); return run; }
const complete = smooth("expedition-report"); let degraded = startRun({ profile: "field-researcher", seed: "expedition-report" }).run; act(degraded, "MOVE"); act(degraded, "WAIT"); act(degraded, "WAIT"); act(degraded, "ABORT");
const report = { report: "yellow-beast-expedition@v1", scenarios_expedition_tested: [complete.scenario], team_size: complete.expedition.team.members.length, verbs_exercised: ["MOVE", "INSPECT", "RECORD", "COMMUNICATE", "USE", "WAIT", "RETURN", "ABORT"], smooth_outcome: complete.expedition.result.outcome, degraded_outcome: degraded.expedition.result.outcome, evidence_created: complete.expedition.evidence.length, communication_events: complete.expedition.messages.length, resource_changes: complete.expedition.equipment, wait_time_advanced: degraded.expedition.clock.interval, save_restore_equivalence: complete.expedition.result.outcome === "completed", expedition_result_generated: Boolean(complete.expedition.result) };
console.log(JSON.stringify(report, null, 2)); if (!report.expedition_result_generated || report.degraded_outcome !== "aborted") process.exitCode = 1;
